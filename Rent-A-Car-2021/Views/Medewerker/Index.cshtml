@model IEnumerable<Rent_A_Car_2021.Models.Medewerker>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Voornaam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tussenvoegsels)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Achternaam)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Voornaam)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tussenvoegsels)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Achternaam)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Medewerkerscode">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Medewerkerscode">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Medewerkerscode">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
